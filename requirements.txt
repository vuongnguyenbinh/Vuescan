# Core dependencies - compatible with Python 3.12/3.13
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
PyQt6-sip>=13.6.0
Pillow>=10.0.0
img2pdf>=0.5.1
pikepdf>=8.0.0
numpy>=1.26.0
reportlab>=4.0.0

# Windows scanner support (only works on Windows)
pytwain>=2.1.0

# Build tools (optional - only needed for creating .exe)
# pip install pyinstaller>=6.0.0
